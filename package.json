{
  "scripts": {
    "build": "node scripts/build.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "execa": "^9.6.1",
    "fs-extra": "^11.3.4",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
